diff -Nru gqview-1.3.4.orig/src/main.c gqview-1.3.4/src/main.c
--- gqview-1.3.4.orig/src/main.c	2003-10-26 10:11:08.000000000 +0100
+++ gqview-1.3.4/src/main.c	2003-11-19 14:20:05.000000000 +0100
@@ -380,8 +380,8 @@
 
 	editor_reset_defaults();
 
-	bookmark_add_default(_("Home"), homedir());
-	path = concat_dir_and_file(homedir(), "Desktop");
+	bookmark_add_default(_("Home"), homedir_old());
+	path = concat_dir_and_file(homedir_old(), "Desktop");
 	bookmark_add_default(_("Desktop"), path);
 	g_free(path);
 	path = concat_dir_and_file(homedir(), GQVIEW_RC_DIR_COLLECTIONS);
diff -Nru gqview-1.3.4.orig/src/ui_bookmark.c gqview-1.3.4/src/ui_bookmark.c
--- gqview-1.3.4.orig/src/ui_bookmark.c	2003-10-26 09:21:27.000000000 +0100
+++ gqview-1.3.4/src/ui_bookmark.c	2003-11-19 14:14:44.000000000 +0100
@@ -427,7 +427,7 @@
 	if (!text)
 		{
 		b->name = g_strdup(_("New Bookmark"));
-		b->path = g_strdup(homedir());
+		b->path = g_strdup(homedir_old());
 		b->key = NULL;
 		return b;
 		}
@@ -842,11 +842,11 @@
 
 		if (!bookmark_default_list)
 			{
-			buf = bookmark_string(_("Home"), homedir(), NULL);
+			buf = bookmark_string(_("Home"), homedir_old(), NULL);
 			history_list_add_to_key(bm->key, buf, 0);
 			g_free(buf);
 
-			path = concat_dir_and_file(homedir(), "Desktop");
+			path = concat_dir_and_file(homedir_old(), "Desktop");
 			if (isname(path))
 				{
 				buf = bookmark_string(_("Desktop"), path, NULL);
diff -Nru gqview-1.3.4.orig/src/ui_fileops.c gqview-1.3.4/src/ui_fileops.c
--- gqview-1.3.4.orig/src/ui_fileops.c	2003-10-31 17:31:32.000000000 +0100
+++ gqview-1.3.4/src/ui_fileops.c	2003-11-19 14:11:07.000000000 +0100
@@ -35,7 +35,7 @@
 gint file_sort_case_sensitive = FALSE;
 
 /* first we try the HOME environment var, if that doesn't work, we try getpwuid(). */
-const gchar *homedir(void)
+const gchar *homedir_old(void)
 {
 	static gchar *home = NULL;
 
@@ -52,6 +52,15 @@
 	return home;
 }
 
+const gchar *homedir(void)
+{
+	static gchar *home_etc = NULL;
+	
+	if (!home_etc) home_etc = g_locale_to_utf8(getenv("HOME_ETC"), -1, NULL, NULL, NULL);
+	if (!home_etc) return homedir_old();
+	return home_etc;
+}
+
 gint stat_utf8(const gchar *s, struct stat *st)
 {
 	gchar *sl;
diff -Nru gqview-1.3.5/src/ui_fileops.h.orig gqview-1.3.5/src/ui_fileops.h
--- gqview-1.3.5/src/ui_fileops.h.orig	2003-11-05 10:55:05.000000000 +0100
+++ gqview-1.3.5/src/ui_fileops.h	2003-11-23 13:28:25.000000000 +0100
@@ -24,6 +24,7 @@
 gchar *path_to_utf8(const gchar *path);
 gchar *path_from_utf8(const gchar *path);
 
+const gchar *homedir_old(void);
 const gchar *homedir(void);
 gint stat_utf8(const gchar *s, struct stat *st);
 gint isname(const gchar *s);
diff -Nru gqview-1.3.4.orig/src/ui_pathsel.c gqview-1.3.4/src/ui_pathsel.c
--- gqview-1.3.4.orig/src/ui_pathsel.c	2003-10-26 09:08:45.000000000 +0100
+++ gqview-1.3.4/src/ui_pathsel.c	2003-11-19 14:15:52.000000000 +0100
@@ -841,7 +841,7 @@
 {
 	Dest_Data *dd = data;
 
-	dest_change_dir(dd, homedir(), (dd->f_view != NULL));
+	dest_change_dir(dd, homedir_old(), (dd->f_view != NULL));
 }
 
 static void dest_show_hidden_cb(GtkWidget *widget, gpointer data)
@@ -1208,7 +1208,7 @@
 			}
 		else
 			{
-			dest_populate(dd, (gchar *)homedir());
+			dest_populate(dd, (gchar *)homedir_old());
 			if (path) gtk_entry_append_text(GTK_ENTRY(dd->entry), "/");
 			if (path) gtk_entry_append_text(GTK_ENTRY(dd->entry), path);
 			}
diff -Nru gqview-1.3.4.orig/src/ui_tabcomp.c gqview-1.3.4/src/ui_tabcomp.c
--- gqview-1.3.4.orig/src/ui_tabcomp.c	2003-10-26 08:38:43.000000000 +0100
+++ gqview-1.3.4/src/ui_tabcomp.c	2003-11-19 14:17:01.000000000 +0100
@@ -140,7 +140,7 @@
 	if (text[0] == '~')
 		{
 		gchar *t = text;
-		text = g_strconcat(homedir(), t + 1, NULL);
+		text = g_strconcat(homedir_old(), t + 1, NULL);
 		g_free(t);
 		}
 
@@ -160,7 +160,7 @@
 	if (text[0] == '~')
 		{
 		gchar *t = text;
-		text = g_strconcat(homedir(), t + 1, NULL);
+		text = g_strconcat(homedir_old(), t + 1, NULL);
 		g_free(t);
 		}
 
@@ -328,7 +328,7 @@
 	/* home dir expansion */
 	if (entry_text[0] == '~')
 		{
-		entry_dir = g_strconcat(homedir(), entry_text + 1, NULL);
+		entry_dir = g_strconcat(homedir_old(), entry_text + 1, NULL);
 		home_exp = TRUE;
 		}
 	else
